import { createContext } from 'react';

export const SelectedObjectContext = createContext(null);
export const ModelPropertiesContext = createContext(null);
export const UserContext = createContext(null);
